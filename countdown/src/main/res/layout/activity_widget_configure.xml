<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:theme="@style/Theme.Countdown"
    android:background="@drawable/app_widget_inner_view_background">

    <EditText
        android:id="@+id/editTextNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:autofillHints="@string/timestamp"
        android:hint="@string/timestamp"/>

    <Button
        android:id="@+id/updateTimeButton"
        style="@style/Widget.Material3.Button"
        android:theme="@style/Theme.Countdown.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/updateTime"
        android:textSize="16sp"/>

    <EditText
        android:id="@+id/targetPlace"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:hint="@string/place"
        android:autofillHints="@string/place"/>

    <Button
        android:id="@+id/updatePlaceButton"
        style="@style/Widget.Material3.Button"
        android:theme="@style/Theme.Countdown.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/updateTitle"
        android:textSize="16sp"/>
</LinearLayout>